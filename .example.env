# Gemini API key used by the BrowserWindow component (client-side). Replace with your key.
NEXT_PUBLIC_GEMINI_API_KEY=<your gemini API>

# Toggle Spotify iframe integration in the MusicPlayer component.
# Set to "false" to disable Spotify (useful for local/dev environments where the
# Spotify embed API cannot be loaded). Defaults to "true" when omitted.
NEXT_PUBLIC_ENABLE_SPOTIFY=true